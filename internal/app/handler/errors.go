package handler

const (
	ErrorMissingRequiredID     = "param `id` is required"
	ErrorMissingAttributeName  = "param `attribute` is required"
	ErrorMissingAttributeValue = "param `value` is required"
	ErrorInvalidRequestPayload = "invalid request payload"
)
